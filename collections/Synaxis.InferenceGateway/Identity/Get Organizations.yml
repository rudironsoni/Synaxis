info:
  name: Get Organizations
  type: http
  seq: 5
http:
  method: GET
  url: "{{baseUrl}}/identity/organizations"
  headers:
    - name: Authorization
      value: Bearer {{token}}
auth:
  mode: bearer
  bearer:
    token: "{{token}}"
