info:
  name: Switch Organization
  type: http
  seq: 6
http:
  method: POST
  url: "{{baseUrl}}/identity/organizations/{{organizationId}}/switch"
  headers:
    - name: Authorization
      value: Bearer {{token}}
auth:
  mode: bearer
  bearer:
    token: "{{token}}"
