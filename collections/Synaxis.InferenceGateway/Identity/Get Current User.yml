info:
  name: Get Current User
  type: http
  seq: 4
http:
  method: GET
  url: "{{baseUrl}}/identity/me"
  headers:
    - name: Authorization
      value: Bearer {{token}}
auth:
  mode: bearer
  bearer:
    token: "{{token}}"
