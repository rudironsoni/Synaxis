info:
  name: Refresh Token
  type: http
  seq: 3
http:
  method: POST
  url: "{{baseUrl}}/identity/refresh"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "refreshToken": "{{refreshToken}}"
      }
auth:
  mode: none
