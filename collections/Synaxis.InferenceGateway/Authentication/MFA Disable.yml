info:
  name: MFA Disable
  type: http
  seq: 12
http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/mfa/disable"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{authToken}}"
  body:
    type: none
auth:
  mode: bearer
  bearer:
    token: "{{authToken}}"
