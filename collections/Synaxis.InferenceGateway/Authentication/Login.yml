info:
  name: Login
  type: http
  seq: 2
http:
  method: POST
  url: "{{baseUrl}}/identity/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "user@example.com",
        "password": "SecurePassword123!"
      }
auth:
  mode: none
