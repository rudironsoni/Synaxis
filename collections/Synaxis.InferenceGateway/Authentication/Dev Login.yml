info:
  name: Dev Login
  type: http
  seq: 3
http:
  method: POST
  url: "{{baseUrl}}/auth/dev-login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "dev@localhost"
      }
auth:
  mode: none
