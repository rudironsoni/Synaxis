info:
  name: MFA Setup
  type: http
  seq: 10
http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/mfa/setup"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{authToken}}"
  body:
    type: none
auth:
  mode: bearer
  bearer:
    token: "{{authToken}}"
