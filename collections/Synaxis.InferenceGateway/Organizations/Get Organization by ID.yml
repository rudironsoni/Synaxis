info:
  name: Get Organization by ID
  type: http
  seq: 1
http:
  method: GET
  url: "{{baseUrl}}/api/v1/organizations/{{organizationId}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
auth:
  mode: bearer
  bearer:
    token: "{{token}}"
