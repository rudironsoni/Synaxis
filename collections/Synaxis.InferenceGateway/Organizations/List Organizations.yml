info:
  name: List Organizations
  type: http
  seq: 3
http:
  method: GET
  url: "{{baseUrl}}/api/v1/organizations?page=1&pageSize=10"
  headers:
    - name: Authorization
      value: Bearer {{token}}
auth:
  mode: bearer
  bearer:
    token: "{{token}}"
