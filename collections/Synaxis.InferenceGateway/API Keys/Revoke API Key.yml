info:
  name: Revoke API Key
  type: http
  seq: 2
http:
  method: DELETE
  url: "{{baseUrl}}/projects/{{projectId}}/keys/{{keyId}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
auth:
  mode: bearer
  bearer:
    token: "{{token}}"
