info:
  name: List API Keys
  type: http
  seq: 4
http:
  method: GET
  url: "{{baseUrl}}/apikeys?includeRevoked=false"
  headers:
    - name: Authorization
      value: Bearer {{token}}
auth:
  mode: bearer
  bearer:
    token: "{{token}}"
