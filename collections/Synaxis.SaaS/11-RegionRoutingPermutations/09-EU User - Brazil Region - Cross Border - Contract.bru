meta {
  name: EU User - Brazil Region - Cross Border - Contract
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/chat/completions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

headers {
  X-User-Region: eu-west-1
  X-Processed-Region: sa-east-1
  X-Cross-Border: true
  X-Legal-Basis: contract
  X-Organization-Id: {{orgId}}
}

body:json {
  {
    "model": "openai/gpt-4",
    "messages": [
      {
        "role": "user",
        "content": "Test message for EU to Brazil transfer for contract"
      }
    ]
  }
}

assert {
  res.status: eq 200
  res.headers.x-cross-border-transfer: eq true
  res.headers.x-legal-basis: eq contract
  res.headers.x-processed-region: eq sa-east-1
}

tests {
  test("Transfer for contract performance", function() {
    expect(res.headers['x-cross-border-transfer']).to.equal('true');
    expect(res.headers['x-legal-basis']).to.equal('contract');
  });
}

docs {
  # EU User - Brazil Region - Cross Border - Contract
  
  Tests EU to Brazil data transfer necessary for contract performance.
  
  ## Legal Basis
  - GDPR Article 49(1)(b) - Contract necessity
  - LGPD Article 33 - Contract execution
}
